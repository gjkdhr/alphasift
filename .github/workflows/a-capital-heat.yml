name: 资金热度

on:
  workflow_dispatch:

  schedule:
    - cron: '25 6 * * 1-5'

jobs:
  screen:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: 安装
        run: |
          pip install -e .

      - name: 资金热度
        run: alphasift screen capital_heat --no-llm
